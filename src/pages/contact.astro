---
import BaseLayout from '../layouts/BaseLayout.astro';
---

<BaseLayout title="Contact" description="Get in touch with Scale Street to discuss AI adoption for your healthcare organization. Based in Athens, Georgia, serving healthcare organizations nationwide.">

  <!-- Contact Page Structured Data -->
  <script slot="head" type="application/ld+json" set:html={JSON.stringify({
    "@context": "https://schema.org",
    "@type": "ContactPage",
    "name": "Contact Scale Street",
    "description": "Get in touch with Scale Street to discuss AI adoption for your healthcare organization.",
    "url": "https://scalestreet.com/contact",
    "mainEntity": {
      "@type": "ProfessionalService",
      "name": "Scale St. Solutions",
      "email": "hello@scalestreet.com",
      "telephone": "+1-706-206-2530",
      "address": {
        "@type": "PostalAddress",
        "addressLocality": "Athens",
        "addressRegion": "GA",
        "addressCountry": "US"
      }
    }
  })} />

  <!-- Page header -->
  <section class="page-hero">
    <div class="page-hero-inner page-hero-grid">
      <div class="hero-content">
        <div class="hero-badge">Get in Touch</div>
        <h1>Let's start a conversation</h1>
        <p>Whether you're exploring AI for the first time or ready to move on a specific initiative, we're here to help. No pitch decks, no pressure — just a practical discussion about what's possible.</p>
      </div>
      <div class="hero-image">
        <img src="/images/medical-consultation.jpg" alt="Healthcare professionals in consultation" />
      </div>
    </div>
  </section>

  <!-- Contact content -->
  <section class="section">
    <div class="section-inner section-center">
      <div class="contact-card">
        <div class="contact-icon">
          <svg width="28" height="28" viewBox="0 0 24 24" fill="none" stroke="white" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M4 4h16c1.1 0 2 .9 2 2v12c0 1.1-.9 2-2 2H4c-1.1 0-2-.9-2-2V6c0-1.1.9-2 2-2z"/><polyline points="22,6 12,13 2,6"/></svg>
        </div>
        <h2>Drop us a line</h2>
        <p>Tell us a bit about your organization and what you're looking for. We'll get back to you within one business day.</p>
        <a href="mailto:hello@scalestreet.com" class="btn btn-primary">Email us at hello@scalestreet.com <span class="arrow">&rarr;</span></a>
      </div>

      <div class="expect-section">
        <h3>What to expect</h3>
        <div class="expect-grid">
          <div class="expect-item">
            <div class="expect-num">1</div>
            <div>
              <strong>A real response within one business day</strong>
              <p>You'll hear back from a real person — not an auto-reply.</p>
            </div>
          </div>
          <div class="expect-item">
            <div class="expect-num">2</div>
            <div>
              <strong>A 30-minute introductory call</strong>
              <p>We'll learn about your organization and discuss where AI might fit.</p>
            </div>
          </div>
          <div class="expect-item">
            <div class="expect-num">3</div>
            <div>
              <strong>A tailored next step</strong>
              <p>Whether that's a readiness assessment, a deeper dive, or just some resources — we'll point you in the right direction.</p>
            </div>
          </div>
        </div>
      </div>

      <div class="details-row">
        <div class="detail-item">
          <div class="detail-label">Email</div>
          <a href="mailto:hello@scalestreet.com" class="detail-value">hello@scalestreet.com</a>
        </div>
        <div class="detail-divider"></div>
        <div class="detail-item">
          <div class="detail-label">Based in</div>
          <span class="detail-value">Athens, Georgia</span>
        </div>
      </div>
    </div>
  </section>

</BaseLayout>

<style>
  .page-hero {
    padding: 5rem 1.5rem 3rem;
    background: var(--color-white);
  }

  .page-hero-inner {
    max-width: var(--max-width);
    margin: 0 auto;
  }

  .page-hero-grid {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 3rem;
    align-items: center;
  }

  .hero-image img {
    width: 100%;
    height: 360px;
    object-fit: cover;
    border-radius: 20px;
  }

  .hero-badge {
    display: inline-block;
    font-size: 0.8125rem;
    font-weight: 700;
    color: var(--color-blue);
    background: rgba(15, 15, 189, 0.07);
    padding: 0.375rem 1rem;
    border-radius: 100px;
    margin-bottom: 1.25rem;
    letter-spacing: 0.01em;
  }

  .page-hero h1 {
    font-size: 2.5rem;
    font-weight: 900;
    color: var(--color-dark);
    margin-bottom: 1rem;
    letter-spacing: -0.02em;
    line-height: 1.1;
  }

  .page-hero p {
    font-size: 1.125rem;
    color: var(--color-gray-500);
    line-height: 1.7;
  }

  /* Contact card */
  .contact-card {
    max-width: 600px;
    margin: 0 auto 3.5rem;
    text-align: center;
  }

  .contact-icon {
    width: 64px;
    height: 64px;
    border-radius: 50%;
    background: var(--color-blue);
    display: flex;
    align-items: center;
    justify-content: center;
    margin: 0 auto 1.5rem;
  }

  .contact-card h2 {
    font-size: 2rem;
    font-weight: 900;
    color: var(--color-dark);
    margin-bottom: 0.75rem;
    line-height: 1.2;
  }

  .contact-card p {
    font-size: 1.0625rem;
    color: var(--color-gray-500);
    line-height: 1.7;
    margin-bottom: 2rem;
  }

  /* Expect section */
  .expect-section {
    max-width: 720px;
    margin: 0 auto 3.5rem;
  }

  .expect-section h3 {
    font-size: 1.25rem;
    font-weight: 700;
    color: var(--color-dark);
    margin-bottom: 1.5rem;
    text-align: center;
  }

  .expect-grid {
    display: flex;
    flex-direction: column;
    gap: 1.25rem;
  }

  .expect-item {
    display: flex;
    gap: 1rem;
    align-items: flex-start;
    background: var(--color-gray-50);
    border-radius: 12px;
    padding: 1.25rem 1.5rem;
  }

  .expect-num {
    width: 1.5rem;
    height: 1.5rem;
    border-radius: 50%;
    background: var(--color-blue);
    color: var(--color-white);
    font-size: 0.8125rem;
    font-weight: 700;
    display: flex;
    align-items: center;
    justify-content: center;
    flex-shrink: 0;
    margin-top: 2px;
  }

  .expect-item strong {
    display: block;
    font-size: 0.9375rem;
    font-weight: 600;
    color: var(--color-gray-800);
    margin-bottom: 0.25rem;
  }

  .expect-item p {
    font-size: 0.875rem;
    color: var(--color-gray-500);
    line-height: 1.5;
  }

  /* Details row */
  .details-row {
    display: flex;
    justify-content: center;
    align-items: center;
    gap: 2.5rem;
  }

  .detail-item {
    text-align: center;
  }

  .detail-label {
    font-size: 0.75rem;
    font-weight: 600;
    text-transform: uppercase;
    letter-spacing: 0.05em;
    color: var(--color-gray-500);
    margin-bottom: 0.25rem;
  }

  .detail-value {
    font-size: 0.9375rem;
    color: var(--color-gray-800);
  }

  a.detail-value:hover {
    color: var(--color-blue);
  }

  .detail-divider {
    width: 1px;
    height: 36px;
    background: var(--color-gray-200);
  }

  @media (max-width: 768px) {
    .page-hero {
      padding: 3.5rem 1.25rem 2.5rem;
    }

    .page-hero-grid {
      grid-template-columns: 1fr;
      gap: 2rem;
    }

    .hero-image img {
      height: 240px;
    }

    .page-hero h1 {
      font-size: 2rem;
    }

    .details-row {
      flex-direction: column;
      gap: 1.5rem;
    }

    .detail-divider {
      width: 40px;
      height: 1px;
    }
  }
</style>
