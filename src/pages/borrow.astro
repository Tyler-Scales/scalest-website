---
import BaseLayout from '../layouts/BaseLayout.astro';
---

<BaseLayout title="Borrow Our Talent" description="Fractional access to Microsoft-certified AI specialists embedded with your healthcare team, on your schedule, at a scope that fits your organization.">

  <!-- Page header -->
  <section class="page-hero">
    <div class="page-hero-inner page-hero-grid">
      <div class="hero-content">
        <div class="hero-badge">Borrow</div>
        <h1>Borrow Our Talent</h1>
        <p>Fractional access to Microsoft-certified AI specialists embedded with your healthcare team, on your schedule, at a scope that fits your organization.</p>
      </div>
      <div class="hero-image">
        <img src="/images/team-collaboration.jpg" alt="Team collaboration on healthcare projects" />
      </div>
    </div>
  </section>

  <!-- Placeholder -->
  <section class="section">
    <div class="section-inner section-center">
      <div class="section-header">
        <h2>Full Borrow services content coming soon.</h2>
        <p>In the meantime, reach out to learn how fractional AI specialists can support your organization.</p>
      </div>
      <a href="/contact" class="btn btn-primary">Get in touch <span class="arrow">&rarr;</span></a>
    </div>
  </section>

</BaseLayout>

<style>
  .page-hero {
    padding: 5rem 1.5rem 3rem;
    background: var(--color-white);
  }

  .page-hero-inner {
    max-width: var(--max-width);
    margin: 0 auto;
  }

  .page-hero-grid {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 3rem;
    align-items: center;
  }

  .hero-image img {
    width: 100%;
    height: 360px;
    object-fit: cover;
    border-radius: 20px;
  }

  .hero-badge {
    display: inline-block;
    font-size: 0.8125rem;
    font-weight: 700;
    color: var(--color-blue);
    background: rgba(15, 15, 189, 0.07);
    padding: 0.375rem 1rem;
    border-radius: 100px;
    margin-bottom: 1.25rem;
    letter-spacing: 0.01em;
  }

  .page-hero h1 {
    font-size: 2.5rem;
    font-weight: 900;
    color: var(--color-dark);
    margin-bottom: 1rem;
    letter-spacing: -0.02em;
    line-height: 1.1;
  }

  .page-hero p {
    font-size: 1.125rem;
    color: var(--color-gray-500);
    line-height: 1.7;
  }

  @media (max-width: 768px) {
    .page-hero {
      padding: 3.5rem 1.25rem 2.5rem;
    }

    .page-hero-grid {
      grid-template-columns: 1fr;
      gap: 2rem;
    }

    .hero-image img {
      height: 240px;
    }

    .page-hero h1 {
      font-size: 2rem;
    }
  }
</style>
